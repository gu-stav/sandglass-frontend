define([ 'jquery',
         'lodash',
         'backbone',
         'models/app' ],
  function( $,
            _,
            Backbone,
            AppModel ) {
  var App = Backbone.Model.extend({

  });

  return App;
});